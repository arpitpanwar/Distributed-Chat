#include "defs.h"
#include<map>
#include<list>
#include<algorithm>
#include<stdlib.h>

class chat_node{

public:
	chat_node(std::string userName,int entry);
	~chat_node();

private :
	std::string userName;
	int entryNum;
	bool isLeader;
	map<string,string> clientmap;
};
